{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  //"DATABASE_URL": "server=localhost; port=5432; database=postgres; user id=wireguard; password=wireguardAdmin",
  "DATABASE_URL": "server=localhost; port=5438; database=postgres; user id=postgres; password=ThisisVeryStrong",

  //docker run -d -p 5438:5432 --name admindb -e POSTGRES_PASSWORD=ThisisVeryStrong --restart always postgres

  "WireguardAdmin": {
    "username": "admin",
    "password": "admin"
  },

  "JWT": {
    "ValidAudience": "User",
    "ValidIssuer": "https://localhost:5001",
    "Secret": "ThisMySecretKey123"
  }
}
